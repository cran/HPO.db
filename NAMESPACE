import(methods)
import(AnnotationDbi)

### Only put what is statically exported here. All the AnnObj instances
### created at load time are dynamically exported (refer to R/zzz.R for
### the details).
export(
    HPO,
    HPO_dbconn,
    HPO_dbfile,
    HPO_dbschema,
    HPO_dbInfo,
    HPOTerms 

)

exportClasses(
      	      HPOTerms,
              HPOTermsAnnDbBimap)
exportMethods(
	#from R/HPOClasses.R file
	HP,  
	Term,
	Synonym
)   
